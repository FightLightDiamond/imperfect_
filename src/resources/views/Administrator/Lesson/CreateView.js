import React from "react";
import CreateContainer from "../../../containers/Administrator/Lesson/CreateContainer";

const CreateView = props => {
    return (
        <div>
            <CreateContainer {...props} />
        </div>
    );
}

export default CreateView
